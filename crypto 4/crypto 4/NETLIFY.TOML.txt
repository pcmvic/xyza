# Salve este código como: netlify.toml
# Na pasta raiz do seu projeto.

[build]
  # Diretório onde os ficheiros do site são publicados.
  # Como o nosso index.html está na raiz, usamos "." para sermos explícitos.
  publish = "."

[functions]
  # Diretório onde as nossas "serverless functions" (o porteiro) estão localizadas.
  directory = "netlify/functions/"
